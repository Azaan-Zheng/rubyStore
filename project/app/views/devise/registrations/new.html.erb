<div class="main_sign">

  <div class="wrapper fadeInDown">
    <div id="formContent">
      <!-- Tabs Titles -->
      <h2 class="inactive underlineHover"> 
        <%= link_to "Sign in", new_session_path(resource_name) %>
      </h2>
      <h2 class="active">Sign Up </h2>

      <!-- Login Form -->
      <%# <form>
        <input type="text" id="login" class="fadeIn second" name="login" placeholder="login">
        <input type="text" id="password" class="fadeIn third" name="login" placeholder="password">
        <input type="submit" class="fadeIn fourth" value="Log In">
      </form> %>

      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>


        <div class="field fadeIn second">
          
          <%= f.text_field :thename, placeholder: "用户名" %>
        </div>

        <div class="field fadeIn third">
          
          <%= f.email_field :email, autofocus: true, placeholder: "邮箱", autocomplete: "email" %>
        </div>

        <div class="field fadeIn third">
          
          <%= f.text_field :thephone, placeholder: "手机号" %>
        </div>

        <div class="field fadeIn fourth">
          
          <!-- 
          <% if @minimum_password_length %>
          <em>(<%= @minimum_password_length %> characters minimum)</em>
          <% end %><br />
          -->

          <%= f.password_field :password, placeholder: "密码（最短6位）", autocomplete: "new-password" %>
        </div>

        <div class="field fadeIn fourth">
          
          <%= f.password_field :password_confirmation, placeholder: "确认密码", autocomplete: "new-password" %>
        </div>

        <div class="actions">
          <%= f.submit "Sign up" %>
        </div>
      <% end %>

      <!-- Remind Passowrd -->
      <%# <div id="formFooter">
        <a class="underlineHover" href="#">Forgot Password?</a>
      </div> %>

    </div>
  </div>



</div>