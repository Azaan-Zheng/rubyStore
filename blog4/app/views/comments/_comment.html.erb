<div id="comment_<%= comment.id %>">
  <p>
    <strong>Posted 
      <%= time_ago_in_words(comment.created_at) %>
    </strong> by <%= link_to comment.user.email, user_path(comment.user) %>.  <br />
    <%= comment.content %>
  </p>
</div>
